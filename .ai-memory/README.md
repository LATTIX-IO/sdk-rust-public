# .ai-memory — Nexus adapter (submodule of lattix-monorepo)

Thin adapter, not the store. Store = external Obsidian vault `50-Memory/`. `repo-profile.md` / `memory-map.md` are committed + repo-safe; `local/` is gitignored.
